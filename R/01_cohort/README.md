# Cohort
The original cohort implementation is python/scripts/stage1_burden.py, function build_cohort(), using pipeline.harmonize(). Do not substitute the older Summary-Stage-restricted cohort. followup_summary.R is the separate authorized descriptive follow-up entry point.

