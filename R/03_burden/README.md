# Conditional burden
The original implementation is python/scripts/stage1_burden.py: AJSpec, estimate(), interval(), result_tables(). Exact jump times and integer patient bootstrap weights are retained. These functions compute both conditional CIF and integrated cause-specific RMTL.

