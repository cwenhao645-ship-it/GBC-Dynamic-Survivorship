# Landmark windows
The original implementation is python/scripts/prepare.py: make_landmarks(), invoked by stage1_burden.long_data(). No duplicate R implementation is created. Analysis time must be strictly greater than landmark time; death at the horizon is included as an event.

